export function avatarURL(file)
{
    return `${process.env.REACT_APP_ASSETS_URL}/avatars/${file}`;
}

export function assetURL(file)
{
    return `${process.env.REACT_APP_ASSETS_URL}/${file}`;
}

export function trailerURL(file)
{
    return `${process.env.REACT_APP_ASSETS_URL}/${file}`;
}
